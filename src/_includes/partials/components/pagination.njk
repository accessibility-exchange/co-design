        <nav class="pagination" aria-label="posts pagination">
            <ul role="list">
            {%- if pagination.href.previous %}
                <li><a href="{{ pagination.href.previous }}">&larr; Previous Page</a></li>
            {%- endif %}
            {%- if pagination.href.next %}
                <li><a href="{{ pagination.href.next }}">Next Page &rarr;</a></li>
            {%- endif %}
            </ul>
        </nav>
