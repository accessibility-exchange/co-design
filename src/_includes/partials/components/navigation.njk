            <nav class="nav" id="menu" aria-label="menu">
                <ul role="list">
                    {% set navItems = collections.all | eleventyNavigation %}
                    {% for item in navItems %}
                    <li>
                        <a href="{{ item.url | url }}"{% if item.url == page.url %} aria-current="page"{% endif %}{% if item.rel %} rel="{{ item.rel }}"{% endif %}>{{ item.title }}</a>
                    </li>
                    {% endfor %}
                </ul>
            </nav>
