{% extends "layouts/base.njk" %}

{% set bodyClass = "updates" %}

{% block content %}
{% if updates.length %}
    <div class="flow">
    {% for item in updates %}
        <article class="entry">
            <h2><a href="{{ item.url }}">{{ item.data.title }}</a></h2>
            <p class="metadata">By <span class="author">{{ author if author else "the Inclusive Design Research Centre" }}</span> – <time datetime="{{ item.data.date | isoDate }}" class="dt-published">{{ item.data.date | formatDate }}</time></p>
            {% if item.data.description %}
            <p>{{ item.data.description | safe }}</p>
            {% endif %}
        </article>
    {% endfor %}
    </div>
    {% if pagination.pages.length > 1 %}
    {% include "partials/components/pagination.njk" %}
    {% endif %}
{% endif %}
{% endblock %}
